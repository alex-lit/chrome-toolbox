.el-alert {
  /* align-items: flex-start;
  border-width: 0;
  border-style: solid;
  border-color: transparent;
  border-radius: 4px;
  box-shadow: inset 0 0 0 1px color-mod(var(--black) alpha(0.06)); */

  & .el-alert__closebtn {
    /* color: currentColor; */
  }
}
